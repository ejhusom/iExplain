services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    volumes:
      - ./config:/app/config
      - ./prompts:/app/prompts
      - ./skills:/app/skills
      - ./runs:/app/runs
      - ./workspaces:/app/workspaces
